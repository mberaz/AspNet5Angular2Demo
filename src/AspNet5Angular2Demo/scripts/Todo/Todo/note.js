var Note = (function () {
    function Note(id, text, color) {
        this.id = id;
        this.text = text;
        this.color = color;
    }
    return Note;
}());
//# sourceMappingURL=note.js.map